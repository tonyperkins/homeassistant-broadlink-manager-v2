# Broadlink Manager - Standalone Docker Configuration
# Copy this file to .env and fill in your values

# ==============================================================================
# REQUIRED CONFIGURATION
# ==============================================================================

# Home Assistant URL
# Use the internal Docker network name if HA is also in Docker, or the host IP
# Examples:
#   - http://homeassistant:8123 (if HA container is named 'homeassistant')
#   - http://192.168.1.100:8123 (if using host network or external HA)
#   - http://localhost:8123 (if HA is on the same host)
HA_URL=http://homeassistant:8123

# Home Assistant Long-Lived Access Token
# Create one in Home Assistant:
#   1. Go to your profile (click your name in the sidebar)
#   2. Scroll down to "Long-Lived Access Tokens"
#   3. Click "Create Token"
#   4. Give it a name like "Broadlink Manager"
#   5. Copy the token and paste it here
HA_TOKEN=your_long_lived_access_token_here

# ==============================================================================
# OPTIONAL CONFIGURATION
# ==============================================================================

# Logging level (trace|debug|info|warning|error|fatal)
# Default: info
LOG_LEVEL=info

# Web interface port
# Default: 8099
WEB_PORT=8099

# Auto-discover Broadlink devices on the network
# Default: true
AUTO_DISCOVER=true

# Home Assistant config directory path (inside container)
# You typically don't need to change this - adjust the volume mount instead
# Default: /config
CONFIG_PATH=/config
